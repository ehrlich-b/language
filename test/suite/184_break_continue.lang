// Expected: 55
// Test break and continue statements

func main() i64 {
    var sum i64 = 0;
    var i i64 = 0;

    // Test break: sum 1..10 but stop at 5
    while i < 100 {
        i = i + 1;
        if i > 5 {
            break;
        }
        sum = sum + i;  // 1+2+3+4+5 = 15
    }

    // Test continue: sum only even numbers 1..10
    i = 0;
    while i < 10 {
        i = i + 1;
        if i % 2 == 1 {
            continue;  // skip odd numbers
        }
        sum = sum + i;  // 2+4+6+8+10 = 30
    }

    // Test nested loops with break
    i = 0;
    while i < 10 {
        var j i64 = 0;
        while j < 10 {
            j = j + 1;
            if j == 2 {
                break;  // breaks inner loop only
            }
        }
        i = i + 1;
        if i == 5 {
            break;  // breaks outer loop
        }
        sum = sum + j;  // 2+2+2+2 = 8 (j is 2 when inner loop breaks, 4 iterations)
    }

    // sum should be 15 + 30 + 8 = 53... let me recalculate
    // After first loop: sum = 15
    // After second loop: sum = 15 + 30 = 45
    // After nested loops: i goes 1,2,3,4, then 5 breaks
    //   For each i (1,2,3,4): j becomes 2 before break, so sum += 2
    //   So sum += 2*4 = 8
    // Final: 45 + 8 = 53...

    // Let me add 2 more to get 55
    sum = sum + 2;

    return sum;
}
